export function generateDecisionExplanation(action: string, reason: string): string {
  return `💡 AI Decision: ${action} because ${reason}. This optimizes muscular growth while mitigating joint strain.`;
}
